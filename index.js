const array = [10, 5, 64];
for (let i = 0; i < array.length; i++) {
  const index = array[i];
  console.log(index);
  // }
  // array.map((a) => console.log(a));

  // for (const a of array) {
  //   console.log(a);
}
